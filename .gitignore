backend/.env
backend/node_modules/
backend/uploads/
backend/logs/
social-feed.json
*.log
php/smtp-config.php
bagelbistro/php/smtp-config.php

# Online ordering — holds DB credentials, Stripe secret key, KDS PIN and the
# printer poll key. Upload to Hostinger by hand via File Manager; it will NOT
# auto-deploy with git. See ORDERING.md.
includes/order-config.php

# Online ordering — SQLite database and its write-ahead log. Live customer
# orders; must never be committed. Web access is denied by db/.htaccess.
db/*.sqlite
db/*.sqlite-wal
db/*.sqlite-shm

# POS register export (19MB, contains clerk/owner data) and build
# intermediates. The generated data/menu.php IS committed; the raw dump is not.
menuprg/

# Python build-tool cache
tools/__pycache__/
