# Bagel Boyz NJ — block direct web access to /db/.
# schema.sql describes the whole database layout. No reason to serve it.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
